# Raw Datasets

This directory contains the original, raw, and untouched datasets. Do NOT modify these files. All cleaning and normalization must be run via the pipeline to output to `data/processed/`.
